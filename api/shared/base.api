syntax = "v1"

@server (
	name:   znp
	prefix: /api/v1
	group:  shared
)
service znp {
	@doc "Service health check"
	@handler Ping
	get /ping returns (PingResponse)
}

type PingResponse {
	status    string
	service   string
	version   string
	site_name string
	logo_url  string
	timestamp int64
}

