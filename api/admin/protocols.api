syntax = "v1"

import "../shared/types.api"

@server (
	name:   znp
	prefix: /api/v1
	group:  admin/protocols
)
service znp {
	@doc "List protocols"
	@handler AdminListProtocols
	get /admin/protocols returns (AdminProtocolListResponse)
}

type AdminProtocolListResponse {
	protocols []string
}
